# syn_interpret dual path: absent ellmer aborts cleanly

    Code
      syn_interpret(sc)
    Condition
      Error:
      ! This feature requires the ellmer package.
      i Install it with `install.packages("ellmer")`.
      i This is optional; the core synesthR workflow does not need it.

