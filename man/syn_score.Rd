% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/score.R
\name{syn_score}
\alias{syn_score}
\title{Score literary text into a prosody_score}
\usage{
syn_score(text, seed = 1L, call = rlang::caller_env())
}
\arguments{
\item{text}{A tibble from \code{\link[=syn_read_text]{syn_read_text()}}.}

\item{seed}{Integer seed stored on the score for reproducible downstream
mapping and rendering. Default \code{1L}.}

\item{call}{The calling environment, for error reporting. Experts only.}
}
\value{
A \code{prosody_score} object: a list with \code{features} (a per-sentence
tibble of \code{sentence_id}, \code{sentence}, \code{valence}, \code{length}, \code{lex_diversity},
\code{punct_density}), \code{arc} (smoothed valence trajectory), \code{lang}, \code{seed}, and
\code{meta}.
}
\description{
Extracts per-sentence linguistic features (valence, length, lexical
diversity, punctuation density) and a global emotional arc from text read by
\code{\link[=syn_read_text]{syn_read_text()}}, returning a \code{prosody_score} object that drives both
\code{\link[=syn_paint]{syn_paint()}} and \code{\link[=syn_play]{syn_play()}}. The result is deterministic: the same text
and \code{seed} always yield an identical score.
}
\examples{
txt <- syn_read_text(syn_example_text("en"), lang = "en")
syn_score(txt)
}
\seealso{
\code{\link[=syn_read_text]{syn_read_text()}} for input, \code{\link[=syn_paint]{syn_paint()}} and \code{\link[=syn_play]{syn_play()}} for output.
}
\concept{scoring functions}
