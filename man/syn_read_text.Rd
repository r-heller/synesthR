% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read.R
\name{syn_read_text}
\alias{syn_read_text}
\title{Read literary text into a tidy tibble}
\usage{
syn_read_text(x, lang = NULL, text_col = "text", call = rlang::caller_env())
}
\arguments{
\item{x}{A single character string, a character vector, a path to an
existing \code{.txt} file, or a tibble/data frame containing a text column.}

\item{lang}{Language code, one of \code{"de"}, \code{"en"}, \code{"fr"}. If \code{NULL} (the
default), the language is auto-detected via \pkg{cld3}. If \pkg{cld3} is
not installed and \code{lang} is \code{NULL}, an error is raised.}

\item{text_col}{When \code{x} is a tibble, the name of the column holding text.}

\item{call}{The calling environment, for error reporting. Experts only.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with columns \code{sentence_id} (integer),
\code{sentence} (character), and \code{lang} (character), plus a \code{lang} attribute.
Empty input returns a zero-row tibble of the same type.
}
\description{
Accepts a character string, a path to a text file, or a tibble with a text
column, and returns a tidy one-row-per-sentence tibble ready for scoring.
The language is auto-detected with the \pkg{cld3} package when available;
otherwise the \code{lang} argument is required.
}
\examples{
syn_read_text("A short tale. It has two sentences.", lang = "en")
}
\seealso{
\code{\link[=syn_score]{syn_score()}} for the next step.

Other reading functions: 
\code{\link{syn_example_text}()}
}
\concept{reading functions}
