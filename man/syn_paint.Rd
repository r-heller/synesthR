% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/paint.R
\name{syn_paint}
\alias{syn_paint}
\title{Render a prosody_score as a generative still image}
\usage{
syn_paint(score, ...)
}
\arguments{
\item{score}{A \code{prosody_score} from \code{\link[=syn_score]{syn_score()}}.}

\item{...}{Reserved for future use; must be empty.}
}
\value{
A \pkg{ggplot2} object.
}
\description{
Builds a generative \pkg{ggplot2} composition from the deterministic visual
parameters mapped from the score. The element cloud, colours, and theme are
all derived from the same score that drives \code{\link[=syn_play]{syn_play()}}, so the picture and
the music are two views of one analysis. The object is returned, never
printed.
}
\examples{
p <- syn_paint(syn_score(syn_read_text(syn_example_text("en"), lang = "en")))
}
\seealso{
\code{\link[=syn_play]{syn_play()}} for the audio view, \code{\link[=syn_animate]{syn_animate()}} for animation.

Other rendering functions: 
\code{\link{syn_animate}()},
\code{\link{syn_play}()},
\code{\link{syn_play_gm}()},
\code{\link{syn_write_wav}()}
}
\concept{rendering functions}
